{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio website",
  "scripts": {
    "start": "live-server --port=8000 --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
